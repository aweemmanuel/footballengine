[project]
name = "football-prediction-engine"
version = "0.1.0"
description = "MVP 31-engine ensemble football prediction system"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "numpy",
  "pandas",
  "pydantic",
  "uvicorn",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
